/**
 * Toast notification component using the sonner library (already installed).
 * This provides the <Toaster /> to place in the app shell,
 * and utility functions to trigger toasts from anywhere.
 */
export { Toaster, toast } from 'sonner';

